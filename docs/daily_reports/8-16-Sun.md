### Milestone
      3.2 Build and Control a Simple Articulated Model

### Current objective
         Create or substantially modify a one-joint or two-joint mechanism, control it from Python, record its state, and explain why it behaves as observed.

### Work Completed

### Commands or tests run
      Task #14

             '''
                unset PYTHONPATH
                source .venv/bin/activate
                python tests/verify_simple_model.py
             '''

             Expected Results:
                    [
                        PASS  test_model_loads
                        PASS  test_worldbody_and_nested_bodies
                        PASS  test_expected_joints_are_hinges
                        PASS  test_joint_limits_are_reasonable
                        PASS  test_expected_actuators_exist
                        PASS  test_actuators_drive_expected_joints
                        PASS  test_actuator_ctrlrange_within_joint_range
                        PASS  test_has_geoms
                        PASS  test_model_steps_without_exploding

                         9 passed, 0 failed
                    ]

### Blockers

           For task 14, after I read it I didn't know write python codes for this task.

           In the given python file, in the medthod: test_model_steps_without_exploding, 
           I didn't understand why estimate a explode in this method: <abs(v) < 1e6 for v in data.qvel>.

### How I investigated

            I asked Claude Code to explain this task and it told me this task is to check the elements in the model created in previous task 8. Furthermore, it wrote the file: tests/verify_simple_model.py.

            Combined with the file: models/simple_arm.xml, I read and understood each segment of codes.

            Then, I sequently asked CLaude Code questions: "In "test_model_steps_without_exploding", why use <all(abs(v) < 1e6 for v in data.qvel)> to estimate whether simulation velocities exploded?" and
            "what is 'NaN', what is '1e6'?"

### AI tools used

                  Claude Code

### What I Learned

        First: what data.qvel actually is. It's the velocity of every joint in the model — for simple_arm.xml, a 2-element array: [shoulder angular velocity, elbow angular velocity], in rad/s. A healthy simulation of a small arm swinging under gravity and mild actuator forces should have velocities somewhere in the single or low double digits — nothing close to a million.

        Second, Why 1e6 specifically — not a physically tight bound, a "this is obviously insane" bound.

        Now, breaking it on purpose — stripping the joint damping and blowing the timestep out to 250x what's in the XML (a classic way to induce genuine numerical instability):

         2159 rad/s is already absurd — this is a tiny arm with 0.3–0.55m links, there's no physical scenario where it should spin that fast. A real numerical blow-up (the kind caused by an actual bug — e.g. someone deleting the damping from your XML, or a bad integration step) typically doesn't stop at hundreds; it compounds exponentially step over step into the millions, or overflows into inf/nan outright. 1e6 sits comfortably above anything a healthy version of this model would ever produce, but well below where a genuine explosion lands — so it has essentially zero false-positive risk while still catching real instability.

         NaN stands for "Not a Number." It's a special floating-point value (part of the IEEE 754 standard that almost all programming languages, including Python, use for decimal numbers) that represents the result of a math operation that has no valid numeric answer — things like 0/0, inf - inf, or the square root of a negative number.

         1e6 is just scientific notation for 1,000,000 (one times ten-to-the-sixth).

### Next action

        3.2 Build and Control a Simple Articulated Model